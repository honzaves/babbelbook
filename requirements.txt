# Babbelbook — Python dependencies
#
# Core (required to run either front-end)
flet>=0.82.0
flask>=3.0

# Format support (optional — install any combination you need)
#   ebooklib   →  .epub metadata and text extraction
#   pymupdf    →  .pdf metadata and text extraction (import as fitz)
#   mobi       →  .mobi / .azw / .azw3 extraction
#   isbnlib    →  ISBN → rich metadata via online APIs
#   langdetect →  language detection from extracted text
ebooklib>=0.18
pymupdf>=1.24
mobi>=0.3.3
isbnlib>=3.10
langdetect>=1.0.9

# Development / testing
pytest>=8.0
pytest-cov>=5.0
